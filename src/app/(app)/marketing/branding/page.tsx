import { MarketingCategoryPage } from "../MarketingCategoryPage";

export default function Page() {
  return (
    <MarketingCategoryPage
      category="BRANDING"
      title="Branding Guidelines"
      subtitle="Brand colours, fonts and usage rules — keep the source guide here so everyone's working from the same one."
    />
  );
}
